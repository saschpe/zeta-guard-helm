# Precompute config snippets for checksum to avoid inline assignment
{{- $bundleCfg := "" }}
{{- if .Values.opa.bundle.enabled }}
{{- $bundleCfg = include "opa.bundleConfigYaml" . }}
{{- end }}
{{- $inlinePolicy := "" }}
{{- if not .Values.opa.bundle.enabled }}
{{- $inlinePolicy = include "opa.policyRego" . }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: opa
  labels:
    {{- include "opa.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.opa.replicaCount }}
  selector:
    matchLabels:
      {{- include "opa.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "opa.labels" . | nindent 8 }}
        {{- with .Values.opa.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- if .Values.opa.podAnnotations | or .Values.devMode }}
      annotations:
        {{- with .Values.opa.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- if .Values.devMode }}
        zeta.dev/rollout-timestamp: "{{ now | unixEpoch }}"
        {{- end }}
      {{- end }}
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      {{- $wif := .Values.opa.workloadIdentityFederation }}
      {{- if .Values.opa.serviceAccountName }}
      serviceAccountName: {{ .Values.opa.serviceAccountName | quote }}
      {{- end }}
      {{- with .Values.opa.imagePullSecrets | default .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: trust-anchor-provisioning-processor
          image: "{{ include "provisioningProcessor.image" . | trim }}"
          imagePullPolicy: "{{ .Values.provisioningProcessor.imagePullPolicy }}"
          {{- with .Values.provisioningProcessor.containerSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: RESULT_DIR
              value: "/var/trust-data"
            - name: PROCESSORS_POLICY_SIGNER_ENABLED
              value: "true"
            - name: PROVISIONING_CONTAINER_NAME
              value: {{ .Values.provisioningProcessor.provisioningContainer | quote }}
            {{- include "zeta-guard.provisioningProcessor.caEnv" . | nindent 12 }}
            - name: TRUST_CERTCHAIN_FILE
              value: "/var/image-trustchain/certchain.pem"
            {{- include "zeta-guard.proxyEnvVars" .Values.global | nindent 12 }}
          {{- with .Values.provisioningProcessor.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: trust-data
              mountPath: "/var/trust-data"
              readOnly: false
            - name: tmpfs
              mountPath: "/tmp"
              readOnly: false
            - name: image-trustchain
              mountPath: "/var/image-trustchain"
              readOnly: true
            {{- include "zeta-guard.provisioningProcessor.caVolumeMounts" . | nindent 12 }}
      containers:
        - name: opa
          {{- with .Values.opa.containerSecurityContext }}
          securityContext:
          {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ include "opa.image" . | trim }}"
          imagePullPolicy: "{{ .Values.opa.imagePullPolicy }}"
          env:
            - name: CHECKSUM_OPA
              value: "{{ list $inlinePolicy (include "opa.configYaml" .) $bundleCfg | toJson | sha256sum }}"
          {{- if and .Values.opa.bundle.enabled .Values.opa.bundle.credentials.secretRef.name }}
            - name: CREDENTIAL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.opa.bundle.credentials.secretRef.name }}
                  key: token
          {{- end }}
            {{- include "zeta-guard.proxyEnvVars" .Values.global | nindent 12 }}
          args:
            - "run"
            - "--server"
            - "--addr=0.0.0.0:8181"
            - "--log-level={{ .Values.opa.logLevel }}"
            - --set-file
            - keys.{{ default "signer" .Values.opa.bundle.verification.keyId }}.key=/var/trust-data/policy-signer.pub
            {{- if or .Values.opaPolicy.logDecisions .Values.opa.bundle.enabled }}
            - "--config-file=/config/opa.yaml"
            {{- end }}
            {{- if not .Values.opa.bundle.enabled }}
            - "/policies/authz.rego"
            {{- end }}
          ports:
            - containerPort: 8181
              name: http
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
            failureThreshold: 3
          {{- with .Values.opa.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            {{- if not .Values.opa.bundle.enabled }}
            - name: opa-policy
              mountPath: /policies/authz.rego
              subPath: authz.rego
              readOnly: true
            {{- end }}
            {{- if or .Values.opaPolicy.logDecisions .Values.opa.bundle.enabled }}
            - name: opa-config
              mountPath: /config/opa.yaml
              subPath: opa.yaml
              readOnly: true
            {{- end }}
            {{- if and .Values.opa.bundle.enabled (and $wif $wif.enabled) }}
            - name: gcp-token-volume
              mountPath: /var/run/secrets/gcp
              readOnly: true
            {{- end }}
            - name: opa-persistence
              mountPath: /var/opa
            - name: trust-data
              mountPath: "/var/trust-data"
              readOnly: true
      volumes:
        {{- if not .Values.opa.bundle.enabled }}
        - name: opa-policy
          configMap:
            name: opa-policy
        {{- end }}
        {{- if or .Values.opaPolicy.logDecisions .Values.opa.bundle.enabled }}
        {{/* Decide if opa-config is a Secret based on presence of SecretRef credentials */}}
        - name: opa-config
          configMap:
            name: opa-config
        {{- end }}
        - name: opa-persistence
          emptyDir: {}
        - name: trust-data
          emptyDir: {}
        - name: tmpfs
          emptyDir:
            medium: "Memory"
        - name: image-trustchain
          secret:
            secretName: {{ required "SecretRef for image verification certchain is missing, see imageTrustCertchainSecretRef" .Values.imageTrustCertchainSecretRef }}
        {{- include "zeta-guard.provisioningProcessor.caVolumes" . | nindent 8 }}
        {{- if and .Values.opa.bundle.enabled (and $wif $wif.enabled) }}
        - name: gcp-token-volume
          secret:
            secretName: opa-gcp-token
        {{- end }}
      {{- with .Values.opa.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.opa.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
